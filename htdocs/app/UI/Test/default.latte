{block content}
<h2>Management of images</h2>
<a n:href="Curator:">as a curator</a><br>
<a n:href="Sign:out">Sign out</a><hr>


<h4>Existing data migration</h4>
1. read Archive Bucket to empty db
<br>
2b.  proceed migration  a pipeline for <u>already existing</u> data

    <pre>
        select count(*), message FROM photos where finalized = 0 AND message IS NOT NULL GROUP BY message;
        UPDATE photos SET message = NULL WHERE message IN ("zbar script error:", "zbar script error", "dimensions error (cache resources exhausted `/srv/temp/curator/prc_421811.jp2' @ error/cache.c/OpenPixelCache/4095)", "dimensions error (cache resources exhausted `/srv/temp/curator/prc_421810.jp2' @ error/cache.c/OpenPixelCache/4095)");
    </pre>

<h4>links</h4>
<ul>
    <li><a href="http://localhost:8080" target="admin">PHPMyadmin</a></li>
</ul>
